# Gitignore for The Giant Movers Website

# ------------------------------------
# 1. Composer Dependencies
# ------------------------------------
# Composer's vendor directory
/vendor/
# Composer's autogenerated files
composer.lock

# ------------------------------------
# 2. Environment Variables (CRITICAL for security)
# ------------------------------------
# Stores sensitive credentials (DB, API keys, SMTP passwords)
.env
# Keep a sample .env file for reference, but don't commit the actual .env
# .env.example

# ------------------------------------
# 3. Log Files
# ------------------------------------
# General log files
*.log
# PHP error logs (if not directed to web server logs)
php_errors.log

# ------------------------------------
# 4. Temporary Files & IDE/Editor Specific Files
# ------------------------------------
# macOS system files
.DS_Store
# Linux thumbnail caches
Thumbs.db
# Various temporary files
*.tmp
*.temp
*.bak
*.swp # Vim swap files
*.swo # Vim swap files

# IDE specific files (adjust as needed for your local development environment)
.idea/           # JetBrains IDEs (IntelliJ, PhpStorm, WebStorm)
.vscode/         # VS Code settings (if not shared)
.sublime-project # Sublime Text project files
.sublime-workspace # Sublime Text workspace files
*.sublime-workspace

# Node.js dependencies (if you use npm for front-end asset compilation)
/node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log

# ------------------------------------
# 5. Backup Files (if created by editors or scripts)
# ------------------------------------
*.bak
*.old
*.orig
*.save

# ------------------------------------
# 6. Uploads/Cache Directories (if applicable, typically managed by web server)
# ------------------------------------
# If you have any user-uploaded content or server-generated cache
# that should NOT be version controlled.
# /uploads/
# /cache/
# /tmp/

# ------------------------------------
# 7. Specific files that might be generated or shouldn't be committed
# ------------------------------------
# If your forms/contact.php or forms/send_quote.php generates any local files, ignore them.
# For example, if it wrote to a file:
# forms/*.txt
# forms/*.csv
